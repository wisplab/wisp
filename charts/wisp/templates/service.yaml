apiVersion: v1
kind: Service
metadata:
  name: {{ include "wisp.fullname" . }}
  labels:
    {{- include "wisp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wisp.selectorLabels" . | nindent 4 }}
